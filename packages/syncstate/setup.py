from setuptools import setup

# Setup stage
setup( packages=[],
    name         = "syncstate",
    version      = "0.0.1",
    description  = "",
    author       = "",
    author_email = "",
    url          = "",
    license      = "",

    data_files   = [
        ('share/nemo-python/extensions', ['syncstate.py'])
    ]
)
